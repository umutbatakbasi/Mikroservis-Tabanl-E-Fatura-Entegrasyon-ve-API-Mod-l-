# Application Settings
APP_NAME="E-Invoice Integration API"
APP_VERSION="1.0.0"
API_V1_PREFIX="/api"
DEBUG=True

# Database Configuration (SQLite for local dev, PostgreSQL/MySQL compatible via connection string)
DATABASE_URL="sqlite:///./invoice.db"

# Invoice Settings
INVOICE_NUMBER_PREFIX="INV"
